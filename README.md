# CourseGraph 项目

## 背后逻辑（离散数学/图论）

- 对象：每个 section（同一门课的不同老师/时间/地点）作为图的节点
- 关系：如果两个 section 的上课时间重叠，则连一条冲突边
- 决策：为每门课选择一个 section，且选择集合中任意两点之间不能有冲突边
- 目标：在可行（无冲突）的前提下，最大化偏好（老师偏好、不早八、不晚课、集中上课、少跨区）
- 输出：Top-3 方案 + 可解释理由（冲突原因、取舍原因）
